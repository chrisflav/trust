{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "moduleResolution": "node",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src"],
  "//": "Tests and their helpers are run by vitest, which resolves modules as ESM, and are the one thing the shipped image has no use for.  `tsconfig.test.json` type-checks them.",
  "exclude": ["src/**/*.test.ts", "src/testing"]
}

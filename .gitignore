/.lake

# Personal working marks; `trust export --marks` points at whichever file you use.
/trust-marks.json

# Where the action writes, when it is run against this repository: the index
# itself, and the second copy of trust that a `trust-ref` asks for.
/trust-index
/.trust-source

# Generated indexes, whatever they are called; tens of megabytes per repository.
/index
